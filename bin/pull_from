#!/bin/bash

set -e

if [ $# == 0 ]; then
  echo "Please specify host."
  exit 2
fi

if [[ $(echo $1 | tr [A-Z] [a-z]) == $(hostname | tr [A-Z] [a-z]) ]]; then
  echo "Same host, idiot."
  exit 2
fi

if [ $# == 2 ]; then
  cd $2
fi

rsync -ave ssh --delete $1:$(pwd)/ .
